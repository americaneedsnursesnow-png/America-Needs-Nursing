# TODO

- [x] Update `HeroSection.tsx` so mobile spacing (top padding / vertical centering) improves without changing large-screen layout.

- [x] Adjust hero flex alignment and padding using Tailwind responsive classes (mobile-only overrides).

- [ ] Adjust form card wrapper width so it stays full width on mobile while keeping existing max-width on desktop.
- [ ] Run front-end dev/build and visually verify on mobile + large breakpoints.

