Hero assets (served from /hero/...)
====================================

  images.jpg   — Home hero background (referenced in src/features/hero/content.ts)

Replace images.jpg with your own wide photo (e.g. 1920×1080+), same filename,
or change `heroContent.image.src` in content.ts to match your file name.

Optional: remove banner.svg if you no longer use it.
