import type { ReactNode } from "react";

/**
 * Route group `(marketing)` — public marketing pages.
 * Same root AppShell/footer; use this file later for marketing-only providers or metadata.
 */
export default function MarketingLayout({ children }: { children: ReactNode }) {
  return children;
}
